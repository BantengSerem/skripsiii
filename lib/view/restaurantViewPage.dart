import 'package:flutter/material.dart';

class RestaurantViewPage extends StatelessWidget {
  const RestaurantViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
